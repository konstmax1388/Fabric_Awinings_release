# Исключения для rsync при выкладке на прод (только рантайм + сборка, без «музейного» хлама).
# С хоста разработки (Linux / macOS / Git Bash):
#   rsync -av --delete --exclude-from=deploy/rsync-exclude.txt ./ user@host:/path/to/app/
#
# На уже залитом сервере одноразово: bash deploy/prune-production-tree.sh /path/to/app

# --- Git / IDE / AI (на проде не нужны для работы сайта) ---
.git/
.github/
.cursor/

# --- Документация и дубликаты репозитория ---
docs/
Fabric_Awinings_react_admin/
CHANGELOG.md
BACKLOG.md
/README.md

# --- Офисные / ТЗ в корне (часто кладут рядом с репо; на хостинг не тащить) ---
*.docx
*.xlsx

# --- Панель staff-ui (дубликат admin-ui; на прод обычно только admin-ui) ---
staff-ui/

# --- Зависимости и кэши (на сервере: pip / npm ci по месту) ---
**/node_modules/
**/.pytest_cache/
**/__pycache__/
.venv/
venv/
backend/.venv/
backend/htmlcov/
htmlcov/
.coverage
coverage/
.nyc_output/

# Тесты на прод не нужны для рантайма
backend/tests/

# Локальные БД и медиа разработчика (на проде свои пути)
*.sqlite3
db.sqlite3
backend/db.sqlite3
backend/media/
media/

# Секреты не копировать с дев-машины — на сервере свой .env
.env
.env.local
.env.*.local
*.pem

# IDE / ОС
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# Локальные выгрузки из ТЗ (см. .gitignore)
docs/_tz*_plain.txt
docs/_content_xlsx.json
docs/_tz20_extract.txt
docs/_xlsx_sample.txt
