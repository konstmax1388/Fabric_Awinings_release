# Исключения для rsync при выкладке на прод (см. корневой README, раздел про деплой).
# Использование (с хоста разработки, Linux/macOS/Git Bash):
#   rsync -av --delete --exclude-from=deploy/rsync-exclude.txt ./ user@host:/path/to/app/

.cursor/
docs/
Fabric_Awinings_react_admin/

# Зависимости и кэши — на сервере: pip install -r requirements.txt; при фронте — npm ci или только dist
**/node_modules/
**/.pytest_cache/
**/__pycache__/
.venv/
venv/
backend/.venv/
backend/htmlcov/
htmlcov/
.coverage
coverage/
.nyc_output/

# Тесты на прод не нужны для рантайма
backend/tests/

# Локальные БД и медиа разработчика (на проде свои пути)
*.sqlite3
db.sqlite3
backend/db.sqlite3
backend/media/
media/

# Секреты не копировать с дев-машины — на сервере свой .env
.env
.env.local
.env.*.local
*.pem

# IDE / ОС
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# Локальные выгрузки из ТЗ (см. .gitignore)
docs/_tz*_plain.txt
docs/_content_xlsx.json
docs/_tz20_extract.txt
docs/_xlsx_sample.txt

# Опционально: не тащить историю git при «толстом» rsync (если деплой не через git pull)
# .git/
