# Что делать дальше (после аудита проекта)

Документ дополняется по мере прогресса. Ориентир по крупным этапам — [rollout-two-phase-plan.md](./rollout-two-phase-plan.md), статус кода — [development-phases.md](./development-phases.md), снимок стека — [project-context.md](./project-context.md). **История релизов и заметных изменений** — [CHANGELOG.md](../CHANGELOG.md). **Беклог задач** — [BACKLOG.md](../BACKLOG.md).

## Недавно сделано (сводка)

См. **[CHANGELOG.md](../CHANGELOG.md)** — актуальный релиз **2.0.4a** (2026-04-14): Staff API + **staff-ui** — контент (портфолио, отзывы, блог, шаблоны писем), загрузка изображений. Предыдущий **2.0.3a**: каркас Staff API и дашборд. **Git:** `git push origin main` (при релизе — тег `git tag -a v2.0.4a`).

## Текущий фокус (апрель 2026)

1. Зафиксировать релиз **2.0.4a** в GitHub; прогнать smoke **staff-ui** (`npm run dev` в `staff-ui/`, порт **17301**) и витрину на хостинге после обновления окружения.
2. Продолжить переход на React Admin (фазы 2+): контент, каталог, настройки — [react-admin-transition-plan.md](./react-admin-transition-plan.md).
3. Подготовить install-пакет под Beget по чеклисту: [beget-install-package-checklist.md](./beget-install-package-checklist.md).
4. По хостингу Beget: дождаться ответа поддержки по окружению **Python 3.12+ / MySQL 8.0+** (текущий сервер: Python 3.10.12, MySQL 5.7 — блокер прямого запуска текущего backend).

## Ближайшие приоритеты (этап 1 магазина)

1. **Закрыть вопросы из [rollout-two-phase-plan.md](./rollout-two-phase-plan.md)** (раздел «Какой информации не хватает»): сущность заказа в Б24, провайдер эквайринга, момент создания отправления СДЭК, маппинг SKU.
2. **Инфраструктура под прод:** **MySQL 8.0.45**, Redis, переменные окружения для БД / Б24 / СДЭК / оплаты; не хранить секреты в Git.
3. **Модель заказа и зеркало в БД** (план §B.1): в коде у `CartOrder` уже есть поля под **статусы выполнения и оплаты**, **Битрикс24** (`bitrix_entity_id`, `bitrix_sync_status`, ошибка/попытки), **доставку** (`delivery_provider`, `delivery_snapshot`, `cdek_tracking`), опциональный **FK на User**; ответ `POST /api/leads/cart/` дополнен `fulfillmentStatus`. Дальше: отдельная модель **адресов покупателя** и регистрация/привязка `user` при ЛК.
4. **Синхронизация каталога с Битрикс24** + локальный overlay (видимость, порядок, SEO) по [tz-2-0-alignment.md](./tz-2-0-alignment.md).
5. **Checkout:** доставка СДЭК + оплата + создание сущности в Б24 после успешной оплаты (или по согласованной политике).
6. **Личный кабинет:** регистрация/вход, история заказов, трек, повтор заказа.

## Технический долг (не блокирует старт интеграций, но стоит запланировать)

- ~~Разбить большой JS-бандл фронта (**lazy routes** / `React.lazy`)~~ — **сделано:** `React.lazy` + `Suspense` в `frontend/src/App.tsx`.
- ~~Динамический **sitemap** по slug товаров и страницам~~ — **сделано:** `GET /sitemap.xml` на бэкенде (`api/views_seo.py`), канонический хост из `DJANGO_PUBLIC_SITE_URL` / `PUBLIC_SITE_URL`.
- На сайте: **lazy load** картинок и **оптимизация изображений** — **частично:** `OptimizedImage` / варианты на API; единый пайплайн для админки и полный CDN-пайплайн — по необходимости.
- Команда **`import_catalog_xlsx`** по [excel-catalog-import.md](./excel-catalog-import.md) для чернового наполнения до полного синка с Б24.
- **Стадия сделки из Битрикс24 на сайте:** обратная синхронизация не реализована (сейчас только отправка заказа в CRM). Отдельная доработка — см. [astrum-bitrix-crm.md](./astrum-bitrix-crm.md) (технический долг).
- **Капча** на публичных формах при росте спама — по необходимости (сейчас honeypot + throttle).

## Зависимости

- **Python:** версии зафиксированы в `backend/requirements.txt` (**Django 6.x**, pytest 9.x). Требуется **Python 3.12+**. На проде БД — **MySQL 8.0.45** (`utf8mb4`); при переносе с SQLite проверить миграции и типы полей.
- **Node:** после `git pull` в `frontend/` выполнять `npm ci` (или `npm install`), затем `npm run build` и `npm run lint`. Панель персонала: в `staff-ui/` — `npm run build` перед релизом артефакта `/staff/`.
